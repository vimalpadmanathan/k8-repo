# Create a POD in namespace:

apiVersion: v1
kind: Pod 
metadata:
  name: demo-pod
  namespace: k8
  labels:
    app: demo
    type: front-end
spec: 
  containers:
  - name: nginx-demo 
    image: nginx:1.20 