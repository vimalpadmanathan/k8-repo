- which helps in creating a new pod if existing is crashed/deleted
- it also helps in load balancing and auto scaling. 
- replication controller is older method and its manage the pods created by replication controller only

- replicaset is newer version 
- replicaset doesn't support rolling update directly, instead we need to use deployment.